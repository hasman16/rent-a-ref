import { GooglerecaptchaDirective } from './googlerecaptcha.directive';

describe('GooglerecaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new GooglerecaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
